language: python

python:
  - 3.6

install:
  - pip install pipenv
  - pipenv sync -d
  - cp contrib/env-sample .env
script:
  - pipenv run flake8 .
  - pipenv run pytest --cov=pypro

after_success:
  - pipenv run codecov